<?php

return array(

	/**
	 * This is the string used to format a Flasher\Message,
	 * when using either the format() method:
	 *
	 * 	$msg->format();
	 *
	 * or when using the magic __toString() method:
	 *
	 * 	echo $msg;
	 *
	 */

	'format' => '<p class="alert alert-:type">:message</p>',

);
